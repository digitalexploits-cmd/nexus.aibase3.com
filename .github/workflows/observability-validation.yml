name: Observability Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    name: Lint · Test · Build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "24"

      - name: Install dependencies
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Unit tests (Vitest)
        run: npm run test -- --reporter=verbose

      - name: Production build
        run: npm run build

      - name: Summary
        if: always()
        run: |
          echo "### Observability Validation" >> $GITHUB_STEP_SUMMARY
          echo "- Validation workflow finished for commit ${{ github.sha }}; see individual step results above." >> $GITHUB_STEP_SUMMARY
